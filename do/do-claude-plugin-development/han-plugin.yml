hooks:
  lint:
    command: "\"${CLAUDE_PLUGIN_ROOT}/hooks/lint.sh\""
    dir_test: "test -f .claude-plugin/marketplace.json"
    if_changed:
      - "**/*.md"
      - ".claude-plugin/**"
      - "commands/**"
      - "skills/**"
  config-sync:
    command: "cd packages/han && bun run lint:config-sync"
    dir_test: "test -d packages/han && test -f packages/han/package.json"
    if_changed:
      - "**/han-plugin.yml"
