[package]
name = "han-proto"
version = "0.1.0"
edition = "2021"
description = "Protobuf definitions for Han coordinator gRPC services"

[dependencies]
tonic = "0.12"
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
