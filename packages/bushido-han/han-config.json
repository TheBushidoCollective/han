{
  "hooks": {
    "build": {
      "command": "bun scripts/build-bundle.js",
      "dirsWith": ["bun.lock"],
      "ifChanged": [
        "lib/**/*.ts",
        "lib/**/*.tsx",
        "lib/**/*.js",
        "lib/**/*.jsx",
        "bun.lock",
        "package.json"
      ]
    }
  }
}
