/**
 * Worktree Sessions Page (/repos/:projectId/worktrees/:worktreeName/sessions)
 */
import SessionListPage from '@/components/pages/SessionListPage';

export default function Page() {
  return <SessionListPage />;
}
