name: Test Dispatch

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Test message'
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Message: ${{ inputs.message }}"
