{
  "hooks": {
    "lint": {
      "command": "npx -y @biomejs/biome check --write --error-on-warnings",
      "dirsWith": ["biome.json"],
      "ifChanged": ["**/*.{js,jsx,ts,tsx,json,jsonc}"]
    }
  }
}
