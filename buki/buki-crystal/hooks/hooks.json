{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with shard.yml -- crystal tool format --check"
          },
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with shard.yml -- crystal spec"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with shard.yml -- crystal tool format --check"
          },
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with shard.yml -- crystal spec"
          }
        ]
      }
    ]
  }
}
