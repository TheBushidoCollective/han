{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"prettier\\\":' package.json\" -- npx prettier --check ."
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"prettier\\\":' package.json\" -- npx prettier --check ."
          }
        ]
      }
    ]
  }
}
