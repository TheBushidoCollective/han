{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with Chart.yaml -- sh -c 'helm lint . && helm template . --debug > /dev/null'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with Chart.yaml -- sh -c 'helm lint . && helm template . --debug > /dev/null'"
          }
        ]
      }
    ]
  }
}
