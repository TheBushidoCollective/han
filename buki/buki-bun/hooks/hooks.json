{
	"hooks": {
		"Stop": [
			{
				"name": "bun-type-check",
				"description": "Validates TypeScript types in Bun projects before stopping",
				"hooks": [
					{
						"type": "command",
						"command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with package.json bun build --target=bun --no-bundle"
					}
				]
			}
		],
		"SubagentStop": [
			{
				"name": "bun-type-check",
				"description": "Validates TypeScript types in Bun projects when agents complete",
				"hooks": [
					{
						"type": "command",
						"command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with package.json bun build --target=bun --no-bundle"
					}
				]
			}
		]
	}
}
