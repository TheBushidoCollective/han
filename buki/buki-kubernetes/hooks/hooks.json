{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with *.yaml,*.yml -- sh -c 'if command -v kubeconform >/dev/null 2>&1; then kubeconform -summary *.yaml *.yml 2>/dev/null || true; fi'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with *.yaml,*.yml -- sh -c 'if command -v kubeconform >/dev/null 2>&1; then kubeconform -summary *.yaml *.yml 2>/dev/null || true; fi'"
          }
        ]
      }
    ]
  }
}
