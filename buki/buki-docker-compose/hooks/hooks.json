{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with docker-compose.*.yaml,docker-compose.*.yml,compose.*.yaml,compose.*.yml -- docker compose config > /dev/null"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with docker-compose.*.yaml,docker-compose.*.yml,compose.*.yaml,compose.*.yml -- docker compose config > /dev/null"
          }
        ]
      }
    ]
  }
}
