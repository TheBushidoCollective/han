{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with rebar.config -- rebar3 compile warnings_as_errors=true"
          },
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with rebar.config -- rebar3 eunit"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with rebar.config -- rebar3 compile warnings_as_errors=true"
          },
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with rebar.config -- rebar3 eunit"
          }
        ]
      }
    ]
  }
}
