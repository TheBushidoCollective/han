{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs -- mix do deps.get, compile --warnings-as-errors"
          },
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with formatter.exs -- mix do deps.get, format --check-formatted"
          },
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs -- mix do deps.get, test --stale"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs -- mix do deps.get, compile --warnings-as-errors"
          },
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with formatter.exs -- mix do deps.get, format --check-formatted"
          },
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs -- mix do deps.get, test --stale"
          }
        ]
      }
    ]
  }
}
