{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with pom.xml --test-dir \"grep -qE 'checkstyle' pom.xml\" -- mvn checkstyle:check"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with pom.xml --test-dir \"grep -qE 'checkstyle' pom.xml\" -- mvn checkstyle:check"
          }
        ]
      }
    ]
  }
}
