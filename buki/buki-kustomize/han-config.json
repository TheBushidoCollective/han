{
  "hooks": {
    "build": {
      "command": "kustomize build . > /dev/null",
      "dirsWith": ["kustomization.yaml"],
      "ifChanged": ["**/*.yaml", "**/*.yml", "kustomization.yaml"]
    }
  }
}
