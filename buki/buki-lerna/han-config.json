{
  "hooks": {
    "lerna-bootstrap": {
      "dirsWith": ["lerna.json"],
      "command": "npx lerna bootstrap --ci 2>/dev/null || npx lerna bootstrap",
      "ifChanged": [
        "**/package.json",
        "lerna.json",
        "package-lock.json",
        "yarn.lock"
      ]
    }
  }
}
