{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with *.tf -- sh -c 'terraform fmt -check && terraform validate'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with *.tf -- sh -c 'terraform fmt -check && terraform validate'"
          }
        ]
      }
    ]
  }
}
