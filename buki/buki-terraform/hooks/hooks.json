{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with *.tf -- sh -c 'terraform fmt -check && terraform validate'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han validate --fail-fast --dirs-with *.tf -- sh -c 'terraform fmt -check && terraform validate'"
          }
        ]
      }
    ]
  }
}
