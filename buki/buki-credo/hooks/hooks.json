{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs --test-dir \"grep -qE ':credo' mix.exs\" -- mix do deps.get, credo --strict"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with mix.exs --test-dir \"grep -qE ':credo' mix.exs\" -- mix do deps.get, credo --strict"
          }
        ]
      }
    ]
  }
}
