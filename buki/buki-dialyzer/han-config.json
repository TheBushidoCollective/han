{
  "hooks": {
    "typecheck": {
      "command": "mix dialyzer",
      "dirsWith": ["mix.exs"],
      "dirTest": "grep -qE ':dialyxir' mix.exs",
      "ifChanged": ["**/*.ex", "**/*.exs", "**/*.eex", "**/*.leex", "**/*.heex", "mix.exs", "mix.lock"]
    }
  }
}
