{
  "hooks": {
    "lint": {
      "command": "bundle exec rubocop",
      "dirsWith": ["Gemfile"],
      "dirTest": "grep -qE '^\\s*gem.*rubocop' Gemfile",
      "ifChanged": ["**/*.rb", "**/*.erb", "Gemfile", ".rubocop.yml", ".rubocop_todo.yml"]
    }
  }
}
