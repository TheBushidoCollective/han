{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"cypress\\\":' package.json\" -- npx cypress run"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"cypress\\\":' package.json\" -- npx cypress run"
          }
        ]
      }
    ]
  }
}
