{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"eslint\\\":' package.json\" -- npx eslint . --max-warnings 0"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with package.json --test-dir \"grep -qE '\\\"eslint\\\":' package.json\" -- npx eslint . --max-warnings 0"
          }
        ]
      }
    ]
  }
}
