{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with pyproject.toml --test-dir \"grep -qE '^pytest[^a-zA-Z]|\\\"pytest\\\"' pyproject.toml\" -- pytest"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "han hook run --fail-fast --dirs-with pyproject.toml --test-dir \"grep -qE '^pytest[^a-zA-Z]|\\\"pytest\\\"' pyproject.toml\" -- pytest"
          }
        ]
      }
    ]
  }
}
