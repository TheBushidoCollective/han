{
  "hooks": {
    "test": {
      "command": "bundle exec rspec",
      "dirsWith": ["Gemfile"],
      "dirTest": "grep -qE '^\\s*gem.*rspec' Gemfile",
      "ifChanged": ["**/*.rb", "**/*.erb", "Gemfile", "Gemfile.lock", "spec/**/*", ".rspec"]
    }
  }
}
