{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with Gemfile --test-dir \"grep -qE 'gem .rspec' Gemfile\" -- bundle exec rspec"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx -y @thebushidocollective/han hook run --fail-fast --dirs-with Gemfile --test-dir \"grep -qE 'gem .rspec' Gemfile\" -- bundle exec rspec"
          }
        ]
      }
    ]
  }
}
