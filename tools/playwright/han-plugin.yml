hooks:
  test:
    command: "npx -y playwright test"
    dirs_with:
      - "playwright.config.ts"
      - "playwright.config.js"
    if_changed:
      - "**/*.{js,ts}x?"
    depends_on:
      - plugin: jutsu-playwright-bdd
        hook: generate
        optional: true
