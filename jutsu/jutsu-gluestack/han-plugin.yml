hooks:
  typecheck:
    command: "npx -y --package typescript tsc --noEmit"
    dirs_with:
      - "gluestack-ui.config.json"
    if_changed:
      - "**/*.{ts,tsx}"
    tip: "Use the `jutsu_gluestack_typecheck` MCP tool before marking complete."
