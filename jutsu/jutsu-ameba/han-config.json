{
  "hooks": {
    "lint": {
      "command": "shards install && ameba",
      "dirsWith": ["shard.yml"],
      "dirTest": "grep -qE 'ameba' shard.yml",
      "ifChanged": ["**/*.cr", "**/*.ecr", "shard.yml", ".ameba.yml"]
    }
  }
}
