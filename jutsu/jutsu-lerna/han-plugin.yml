hooks:
  lerna-bootstrap:
    dirs_with:
      - "lerna.json"
    command: "npx -y lerna bootstrap --ci 2>/dev/null || npx -y lerna bootstrap"
    if_changed:
      - "**/package.json"
      - "lerna.json"
      - "package-lock.json"
      - "yarn.lock"
