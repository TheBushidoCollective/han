hooks:
  check_commits:
    command: "git diff --stat --quiet 2>/dev/null && git diff --cached --stat --quiet 2>/dev/null && echo 'No uncommitted changes' || echo '⚠️ Uncommitted changes detected. Consider committing to tell the story of your development.'"
    description: "Commit work early and often for git storytelling"
