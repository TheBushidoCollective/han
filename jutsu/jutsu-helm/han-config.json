{
  "hooks": {
    "lint": {
      "command": "helm lint . && helm template . --debug > /dev/null",
      "dirsWith": ["Chart.yaml"],
      "ifChanged": [
        "**/*.yaml",
        "**/*.yml",
        "**/*.tpl",
        "Chart.yaml",
        "values.yaml"
      ]
    }
  }
}
