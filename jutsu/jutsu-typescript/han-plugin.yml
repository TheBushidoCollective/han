hooks:
  typecheck:
    command: "npx -y --package typescript tsc"
    dirs_with:
      - "tsconfig.json"
    if_changed:
      - "**/*.{ts,tsx,mts,cts}"
      - "tsconfig*.json"
    tip: "Use the `jutsu_typescript_typecheck` MCP tool during development, not just at completion."
