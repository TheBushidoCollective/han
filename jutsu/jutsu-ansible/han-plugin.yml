hooks:
  lint:
    command: "if command -v ansible-lint >/dev/null 2>&1; then ansible-lint *.yml *.yaml 2>/dev/null || true; fi"
    dirs_with:
      - "*.yml,*.yaml"
    if_changed:
      - "**/*.yml"
      - "**/*.yaml"
      - "ansible.cfg"
