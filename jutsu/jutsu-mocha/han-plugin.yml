hooks:
  test:
    command: "npm test"
    dirs_with:
      - "package.json"
    dir_test: "grep -qE '\"mocha\":' package.json"
    if_changed:
      - "**/*.{js,jsx,ts,tsx,mjs,cjs}"
      - "test/**/*"
      - ".mocharc.*"
