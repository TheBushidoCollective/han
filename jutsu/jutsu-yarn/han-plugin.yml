hooks:
  install:
    dirs_with:
      - "yarn.lock"
    command: "yarn install --check-files --frozen-lockfile 2>/dev/null || yarn install"
    if_changed:
      - "**/package.json"
      - "yarn.lock"
