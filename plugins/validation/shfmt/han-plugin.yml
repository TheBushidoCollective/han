hooks:
  format:
    event: [Stop, PostToolUse:Edit|Write]
    command: "shfmt -d ${HAN_FILES}"
    dirs_with:
      - ".shfmt.toml"
      - "shfmt.toml"
      - "*.sh"
    if_changed:
      - "**/*.sh"
      - "**/*.bash"
      - "**/.[!.]*"
