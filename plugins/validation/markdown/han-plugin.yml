hooks:
  lint:
    event: [Stop, PostToolUse:Edit|Write]
    command: "npx -y markdownlint-cli --fix ${HAN_FILES}"
    dirs_with:
      - "'.markdownlint.*'"
    if_changed:
      - "**/*.md"
      - ".markdownlint.*"
    file_filter:
      - "**/*.md"
