hooks:
  validate:
    command: act --dryrun
    timeout: 120000  # 2 minutes - act needs time to pull Docker images
    dirs_with:
      - ".github/workflows"
    if_changed:
      - ".github/workflows/**/*.yml"
      - ".github/workflows/**/*.yaml"
